# This is a simple workflow to test the initialisation on linux.

name: Linux
on:
  push:
  pull_request:

jobs:
  build:

    strategy:
      fail-fast: false
      matrix:
        python-version: [3.12]
        platform: [ubuntu-latest]
    runs-on: ${{ matrix.platform }}

    steps:
      - uses: actions/checkout@v4

      - name: Set up
        run: |
          pipx install poetry
          echo "Setting up environment"
          echo "Python version: ${{ matrix.python-version }}"
          echo "Platform: ${{ matrix.platform }}"
          
          source setup.sh
